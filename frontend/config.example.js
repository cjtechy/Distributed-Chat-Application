// Copy to config.js (or set the API_BASE repo variable for GitHub Pages deploy).
window.APP_CONFIG = {
  API_BASE: "https://api.yourdomain.com/v1",
  // Optional: Erlang/OTP WebSocket endpoint.
  // WS_BASE: "wss://ws.yourdomain.com/v1",
};
